// Fill this in with your Firebase Functions base URL, e.g.
//   "https://us-central1-tai-invoice-automation.cloudfunctions.net"
// You can find it by running `firebase deploy --only functions` and copying
// the host from any of the printed function URLs (drop the function name).
window.DASHBOARD_CONFIG = {
  FUNCTIONS_BASE_URL: "https://us-central1-tai-invoice-automation.cloudfunctions.net",
};
